# ==============================================================================
# İSLÂMİ UYGULAMA DOĞRULUK DENETÇİSİ - KÜTÜPHANE BAĞIMLILIKLARI (REQUIREMENTS)
# ==============================================================================
# Bu dosya, projemizin çalışması için gerekli dış Python kütüphanelerini listeler.
# Yüklemek için terminalde: pip install -r requirements.txt

requests>=2.31.0      # Ollama API'si ve web/Hadis API'lerine HTTP isteği (POST/GET) atmak için
adhanpy>=1.0.4        # Namaz vakitlerini Diyanet, ISNA, MWL vb. yöntemlerle astronomik hesaplayan kütüphane
hijri-converter>=2.3.1 # Miladi takvim ile Hicri takvim (Umm al-Qura) arasında hatasız tarih dönüşümü için
chromadb>=0.4.24      # Vektör veritabanı (RAG katmanında fıkhi kuralları ve dokümanları vektör olarak saklar)
